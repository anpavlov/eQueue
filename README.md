# eQueue front for ADMIN
Requirements:
```
nodejs >= 4.0.0
```
Before doing anything (in project dir):
```
npm install
```

To run dev server:
```
npm run dev-server
```
To compile into `dist` dir:
```
npm run compile
```
