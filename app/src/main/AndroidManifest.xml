<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sudo.equeue" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.QueueApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".activities.StartActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".NetService"
            android:exported="false" >
        </service>

        <!-- <provider -->
        <!-- android:name=".content.HHContentProvider" -->
        <!-- android:authorities="com.example.alex.headhunter.provider" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="false" > -->
        <!-- </provider> -->

        <activity android:name=".activities.MainActivity" >
        </activity>
        <activity android:name=".activities.QueueAdminActivity" >
        </activity>
        <activity android:name=".activities.QueueViewerActivity" >
        </activity>
        <activity android:name=".activities.LoginActivity" >
        </activity>
        <activity android:name=".activities.RegisterActivity" >
        </activity>
        <activity android:name=".activities.WebViewActivity" >
        </activity>
    </application>

</manifest>
